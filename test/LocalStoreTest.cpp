#include <RRDTP/LocalStore.h>
#include <cassert>

int main()
{
	/*rrdtp::LocalStore localStore;

	//Test get/set 32-bit integer value with no category specified.
	localStore.SetInt("test-int", 12345);
	assert(localStore.GetInt("test-int") == 12345);

	//Test get/set 32-bit integer value with category specified.
	localStore.SetInt("category1.test-int", 54321);
	assert(localStore.GetInt("category1.test-int") == 54321);

	//Test get/set 32-bit integer value with no category specified.
	localStore.SetLong("test-long", 12345);
	assert(localStore.GetLong("test-long") == 12345);

	//Test get/set 32-bit integer value with category specified.
	localStore.SetLong("category1.test-long", 54321);
	assert(localStore.GetLong("category1.test-long") == 54321);*/

	return 0;
}